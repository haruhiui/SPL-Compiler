program test_char;

var
    letter : char;

begin
    for letter := 'a' to 'z' do 
        if (ord(letter) - ord('a')) mod 2 = 0 
        then writeln(letter);
    for letter := 'z' downto 'a' do
        if (ord(letter) - ord('z')) mod 2 = 0
        then writeln(letter);
end.